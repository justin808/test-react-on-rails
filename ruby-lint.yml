presenter: syntastic

# These get added to whatever you specify on the command line!
directories:
  - app
  - config
# Enable if using rspec
#  - spec

ignore_paths:
  - client
  - node_modules
  - client/node_modules

analysis_classes:
  #  - argument_amount
  - pedantics
  - shadowing_variables
  # Too many false positives for these
  #  - undefined_methods
  #  - undefined_variables
  #  - unused_variables
  - useless_equality_checks
